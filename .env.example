# Supabase Postgres connection strings
# Supabase dashboard → Project Settings → Database → Connection string

# Pooled connection (Transaction mode, PgBouncer on port 6543).
# Used by the app at runtime — required for serverless on Vercel.
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (Session mode, port 5432).
# Used by Prisma for migrations and schema pushes only.
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"
